import './style.css'

// const leftButton = document.querySelector("#sliderLeft");
// const rightButton = document.querySelector("#sliderRight");

// function slideRight() {
//     let gameCards = document.body.querySelectorAll(".gameCard");
//     let cardNo = (gameCards.length - 1);
//     gameCards[cardNo].classList.add("gameCard__shrunk");
//     setTimeout(() => {document.querySelector(".footer-slider-cardContainer")?.prepend(gameCards[cardNo])}, 500);
//     setTimeout(() => {gameCards[cardNo].classList.remove("gameCard__shrunk")}, 550);
// };

// function slideLeft() {
//     let gameCards = document.body.querySelectorAll(".gameCard");
//     let cardNo = 0;
//     gameCards[cardNo].classList.add("gameCard__shrunk");
//     setTimeout(() => {document.querySelector(".footer-slider-cardContainer")?.append(gameCards[cardNo])}, 500);
//     setTimeout(() => {gameCards[cardNo].classList.remove("gameCard__shrunk")}, 550);
// };

// leftButton?.addEventListener("click", slideLeft);
// rightButton?.addEventListener("click", slideRight);
